# StorySitting private config

This directory is for server-only runtime configuration.

Do not commit live credentials, private keys, admin access files, API keys, database config, or generated lead data to this repository.

For the ops dashboard, create the required server-only access file directly in cPanel File Manager or SSH on the live server. Keep the live file out of GitHub.
